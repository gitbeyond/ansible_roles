
civp_run_user: tomcat
civp_run_group: "{{civp_run_user}}"

#
# global 
app_base_dir: /data/apps/opt

mv_log_script: "mv_log.sh"
# tomcat ssh
# ssh client, ip
civp_ssh_host:
# ssh server, ip list
civp_ssh_server_host:

civp_prom_conf: jmx_civp_prometheus.yml
