{
  "CN": "{{etcd_cert_member_ca_cn}}",
  "hosts": {{etcd_cert_hosts | to_nice_json}},
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "{{etcd_cert_c}}",
      "ST": "{{etcd_cert_st}}",
      "L": "{{etcd_cert_l}}",
      "O": "{{etcd_cert_o}}",
      "OU": "{{etcd_cert_ou}}"
    }
  ]
}
