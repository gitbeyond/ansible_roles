# jdk_packet 须是 tar.gz 的格式
jdk_packet: ""
# 这个变量一般取自全局变量 {{app_base_dir}}
jdk_app_name: "java"
app_base_dir: "/data/apps/opt"
jdk_install_dir: "{{app_base_dir}}"
jdk_base_dir: "{{jdk_install_dir}}/{{jdk_app_name}}"
# 非软链的基本目录，运行中生成
jdk_raw_base_dir: ""
jdk_create_link_name: true
jdk_profile: "java.sh"

jdk_certs: []
# - cert_alias: abc.domain.com
#   cert_url: 'https://abc.domain.com'
#   cert_port: 443
# - cert_alias: def.domain.com
#   cert_path: '/var/certs/def.cert'
