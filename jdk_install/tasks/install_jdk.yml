---
- name: install jdk
  unarchive: src={{ item.src }} dest={{ item.dest }} owner={{ item.owner }} group={{ item.group }} creates={{ item.creates }}
  with_items:
    - {src: '{{ jdk_packet }}', dest: '{{install_dir}}/', owner: root, group: root, creates: '{{install_dir}}/{{jdk_dir_name}}'}
