---
- name: copy filebeat conf file
  template: 
    src: '{{ item }}'
    dest: '{{filebeat_conf_dir}}/inputs.d/' 
    owner: root 
    group: root 
    mode: "0644" 
    backup: yes
  with_items: "{{filebeat_confs}}"
  when: filebeat_confs is not none

