
common_run_user: '{{mongo_run_user}}'
common_base_name: '{{mongo_base_name}}'
common_base_dir: '{{mongo_base_dir}}'
common_log_dir: '{{mongo_log_dir}}'
common_conf_dir: '{{mongo_conf_dir}}'
common_var_dir:  '{{mongo_var_dir}}'
common_data_dir: '{{mongo_data_dir}}'
common_child_conf_dir: '{{mongo_child_conf_dir}}'
common_packet: '{{mongo_packet}}'
common_packet_creates: '{{mongo_packet_creates}}'
common_packet_type: '{{mongo_packet_type}}'

common_bin_scripts: '{{mongo_bin_scripts}}'
common_child_confs: '{{mongo_child_confs}}'
common_conf_files: '{{mongo_conf_files}}'
common_env_file: '{{mongo_env_file}}'

common_src_boot_file: '{{mongo_src_boot_file}}'
common_boot_type: '{{mongo_boot_type}}'
