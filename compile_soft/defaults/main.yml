## 
app_base_name: 
soft_base_dir: '/data/apps/soft/ansible/{{app_base_name}}'
app_source_package:
app_compilation_user: root
# 有时候，直接给出了源代码目录
app_source_dir: 
app_compilation_dependent_soft: 
## mysql
mysql_source_package:
mysql_compilation_cmd: 
