- name: add to service list
  shell: '{{ item }}'
  with_items:
    - systemctl daemon-reload
- name: supervisor restarted
  supervisorctl:
    name: "{{exporter_instance_name}}"
    state: restarted
