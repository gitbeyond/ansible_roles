---
- name: copy nodejs profile
  template: 
    src: '{{ item.src }}' 
    dest: '{{ item.dest }}' 
    owner: root 
    group: root 
    mode: "0644"
  loop:
    - src: 'nodejs.sh'
      dest: '/etc/profile.d/{{nodejs_app_name}}.sh'
