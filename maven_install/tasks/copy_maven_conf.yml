- name: copy profile to remote
  template:
    src: "{{maven_conf_file}}"
    dest: "{{ maven_install_dir }}/{{maven_dir_name.stdout}}/conf/settings.xml"
    owner: root
    group: root
    mode: "0644"
  when:
    - maven_conf_file != ""
